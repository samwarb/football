{
  "name": "premier-league-ledger",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-pages.js",
    "start": "node server/index.js",
    "test": "node --test",
    "worker:dev": "wrangler dev",
    "worker:deploy": "wrangler deploy",
    "worker:kv:create": "wrangler kv namespace create FOOTBALL_CACHE",
    "worker:smoke": "node scripts/smoke-worker.js",
    "ios:icon": "node scripts/generate-ios-icon.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
